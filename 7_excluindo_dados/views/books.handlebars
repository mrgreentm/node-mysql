<div>
    {{#each books}}
        <p>{{this.title}} tem {{this.pages}} páginas</p>
        <a href="/books/{{this.id}}">Acessar</a>
        <a href="/books/edit/{{this.id}}">Editar</a>
        <form action="/books/remove/{{this.id}}" method="POST">
            <input type="submit" value="remover">
        </form>
    {{/each}}
</div>